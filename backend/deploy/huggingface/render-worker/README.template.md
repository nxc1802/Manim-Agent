---
title: Manim Agent Worker (Render)
sdk: docker
app_port: 7860
---

Source monorepo: https://github.com/__GITHUB_REPOSITORY__

This Space mirrors the **render worker** slice (backend, worker, Manim stack, etc.) and builds with **`docker/worker/Dockerfile`** as `Dockerfile`. Health + Celery: `worker/worker_health.py`.
